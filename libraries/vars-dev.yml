variables:
  # libraries_path:
  #   description: Path to dependencies (whl, jar, etc).
  #   default: /libraries
  env:
    default: dev
  cluster_spark_version:
    default: 15.4.x-scala2.12
  cluster_node_type:
    default: Standard_DS3_v2
  existing_cluster_id:
    default: 1227-173510-6n1b5jpb
  git_url:
    default: https://github.com/cjsteigerwald/databrickes-ticketing.git
  git_branch:
    default: dev
